Audit this Astro project for Frontmatter Solo compatibility.

Follow fm-skill.md strictly.

Use this starter as the preferred reference for:
- component structure
- naming conventions
- page composition
- layout patterns

[Frontmatter Solo Starter](https://github.com/withfrontmatter/fm-solo-starter)

Tasks:
1. list all contract violations
2. group them by type:
   - layout
   - props
   - rendering
   - CSS
   - runtime features
   - data loading
3. explain why each violation is incompatible
4. propose the smallest valid fix for each one

Prefer existing starter patterns over inventing new component abstractions.

Do NOT rewrite everything unless necessary.
Be precise.
Be strict.
Do not pretend incompatible code is acceptable.

Target contract:

frontmatter solo:validate
